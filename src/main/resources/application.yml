spring:
  jackson.date-format: yyyy-MM-dd HH:mm:ss
  datasource:
    url: jdbc:postgresql://localhost:5445/upp_udd
    username: upp_udd_user
    password: upp_udd_password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties.hibernate:
      temp.use_jdbc_metadata_defaults: false  # explanation in README file and on confluence as well
      jdbc.lob.non_contextual_creation: true

activiti-cloud-modeling:
  enabled: true

elasticsearch:
  clustername: elasticsearch
  host: localhost
  port: 9300


upload.directory: /Users/babic/git/upp_udd/articles/
server:
  port: 8081

spring.jpa.hibernate.ddl-auto: create
